interface:
  display_name: "/xxd-panel-047"
  short_description: "Create Panel 047 from its original style brief."
  default_prompt: "Use $xxd-panel-047; keep the body of its bundled original style brief as the sole aesthetic authority, send exactly one mode-specific delivery contract per asset, make design-only and wallpaper outputs fully transformed, and ground factual-looking copy in supplied or verified facts."

policy:
  allow_implicit_invocation: true
